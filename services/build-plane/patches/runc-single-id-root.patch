diff --git a/libcontainer/configs/config_linux.go b/libcontainer/configs/config_linux.go
index 65cbcad..f80b50a 100644
--- a/libcontainer/configs/config_linux.go
+++ b/libcontainer/configs/config_linux.go
@@ -4,4 +4,5 @@ import (
 	"errors"
 	"fmt"
 	"math"
+	"os"
 )
@@ -51,8 +52,16 @@ func (c Config) HostUID(containerId int) (int, error) {
 	return containerId, nil
 }
 
+func IsLrailSingleIDMap(mappings []IDMap) bool {
+	return os.Getenv("LRAIL_ROOTLESSKIT_SINGLE_ID") == "true" && len(mappings) == 1 &&
+		mappings[0] == (IDMap{ContainerID: 10001, HostID: 0, Size: 1})
+}
+
 // HostRootUID gets the root uid for the process on host which could be non-zero
 // when user namespaces are enabled.
 func (c Config) HostRootUID() (int, error) {
+	if c.Namespaces.Contains(NEWUSER) && IsLrailSingleIDMap(c.UIDMappings) {
+		return 0, nil
+	}
 	return c.HostUID(0)
 }
@@ -81,5 +90,8 @@ func (c Config) HostGID(containerId int) (int, error) {
 // HostRootGID gets the root gid for the process on host which could be non-zero
 // when user namespaces are enabled.
 func (c Config) HostRootGID() (int, error) {
+	if c.Namespaces.Contains(NEWUSER) && IsLrailSingleIDMap(c.GIDMappings) {
+		return 0, nil
+	}
 	return c.HostGID(0)
 }
diff --git a/libcontainer/configs/config_linux_test.go b/libcontainer/configs/config_linux_test.go
index 4a8461f..8688c16 100644
--- a/libcontainer/configs/config_linux_test.go
+++ b/libcontainer/configs/config_linux_test.go
@@ -78,3 +78,28 @@ func TestHostRootGIDWithUSERNS(t *testing.T) {
 		t.Fatalf("expected gid 1000 with no USERNS but received %d", uid)
 	}
 }
+
+func TestHostRootIDsWithLrailSingleIDMapping(t *testing.T) {
+	t.Setenv("LRAIL_ROOTLESSKIT_SINGLE_ID", "true")
+	config := &Config{
+		Namespaces: Namespaces{{Type: NEWUSER}},
+		UIDMappings: []IDMap{{ContainerID: 10001, HostID: 0, Size: 1}},
+		GIDMappings: []IDMap{{ContainerID: 10001, HostID: 0, Size: 1}},
+	}
+	uid, uidErr := config.HostRootUID()
+	gid, gidErr := config.HostRootGID()
+	if uidErr != nil || gidErr != nil || uid != 0 || gid != 0 {
+		t.Fatalf("host root IDs = %d, %d, %v, %v", uid, gid, uidErr, gidErr)
+	}
+}
+
+func TestHostRootIDsRequireExactLrailSingleIDMapping(t *testing.T) {
+	t.Setenv("LRAIL_ROOTLESSKIT_SINGLE_ID", "true")
+	config := &Config{
+		Namespaces: Namespaces{{Type: NEWUSER}},
+		UIDMappings: []IDMap{{ContainerID: 10002, HostID: 0, Size: 1}},
+	}
+	if _, err := config.HostRootUID(); err == nil {
+		t.Fatal("unexpected root alias for a different mapping")
+	}
+}
diff --git a/libcontainer/nsenter/nsexec.c b/libcontainer/nsenter/nsexec.c
index 728e68b..2922683 100644
--- a/libcontainer/nsenter/nsexec.c
+++ b/libcontainer/nsenter/nsexec.c
@@ -17,11 +17,13 @@
 #include <unistd.h>
 
 #include <sys/ioctl.h>
+#include <sys/syscall.h>
 #include <sys/prctl.h>
 #include <sys/socket.h>
 #include <sys/types.h>
 #include <sys/wait.h>
 
+#include <linux/capability.h>
 #include <linux/limits.h>
 #include <linux/netlink.h>
 #include <linux/types.h>
@@ -52,6 +54,55 @@ enum sync_t {
 /* Stores the current stage of nsexec. */
 int current_stage = STAGE_SETUP;
 
+#define LRAIL_SINGLE_ID 10001
+
+static bool lrail_single_id(void)
+{
+	const char *value = getenv("LRAIL_ROOTLESSKIT_SINGLE_ID");
+	return value != NULL && strcmp(value, "true") == 0;
+}
+
+static void verify_lrail_single_id_identity(void)
+{
+	uid_t ruid, euid, suid;
+	gid_t rgid, egid, sgid;
+	if (getresuid(&ruid, &euid, &suid) < 0 ||
+	    getresgid(&rgid, &egid, &sgid) < 0)
+		bail("failed to inspect strict single-ID credentials");
+	if (ruid != LRAIL_SINGLE_ID || euid != LRAIL_SINGLE_ID || suid != LRAIL_SINGLE_ID ||
+	    rgid != LRAIL_SINGLE_ID || egid != LRAIL_SINGLE_ID || sgid != LRAIL_SINGLE_ID)
+		bail("strict single-ID credentials differ from the exact logical identity");
+}
+
+static void restore_lrail_setup_capabilities(void)
+{
+	struct __user_cap_header_struct header = { _LINUX_CAPABILITY_VERSION_3, 0 };
+	struct __user_cap_data_struct data[_LINUX_CAPABILITY_U32S_3] = { 0 };
+	const unsigned int index = CAP_SYS_ADMIN / 32;
+	const __u32 mask = ((__u32) 1U) << (CAP_SYS_ADMIN % 32);
+	if (syscall(SYS_capget, &header, &data) < 0)
+		bail("failed to inspect strict single-ID setup capabilities");
+	if ((data[index].permitted & mask) == 0)
+		bail("strict single-ID namespace lacks permitted setup authority");
+	for (size_t i = 0; i < _LINUX_CAPABILITY_U32S_3; i++)
+		data[i].effective = data[i].permitted;
+	if (syscall(SYS_capset, &header, &data) < 0)
+		bail("failed to restore strict single-ID setup capabilities");
+}
+
+static pid_t lrail_procfs_pid(void)
+{
+	char path[64];
+	int pid, tid;
+	ssize_t length = readlink("/proc/thread-self", path, sizeof(path) - 1);
+	if (length <= 0 || (size_t) length >= sizeof(path))
+		bail("failed to translate strict single-ID process through procfs");
+	path[length] = '\0';
+	if (sscanf(path, "%d/task/%d", &pid, &tid) != 2 || pid <= 0 || pid != tid)
+		bail("strict single-ID procfs process identity is invalid");
+	return pid;
+}
+
 /* Assume the stack grows down, so arguments should be above it. */
 struct clone_t {
 	/*
@@ -541,8 +592,12 @@ static nsset_t __join_namespaces(nsset_t allow, struct namespace_t *ns_list, siz
 		 * <https://github.com/opencontainers/runc/issues/4466> for one example.
 		 */
 		if (type == CLONE_NEWUSER) {
-			if (setresuid(0, 0, 0) < 0)
+			if (lrail_single_id()) {
+				verify_lrail_single_id_identity();
+				restore_lrail_setup_capabilities();
+			} else if (setresuid(0, 0, 0) < 0) {
 				bail("failed to become root in user namespace");
+			}
 		}
 
 		close(ns->fd);
@@ -828,4 +883,4 @@ void nsexec(void)
 	case STAGE_PARENT:{
 			int len;
-			pid_t stage1_pid = -1, stage2_pid = -1;
+			pid_t stage1_pid = -1, stage2_pid = -1, stage2_procfs_pid = -1;
 			bool stage1_complete, stage2_complete;
@@ -854,3 +909,4 @@ void nsexec(void)
 			while (!stage1_complete) {
 				enum sync_t s;
+				pid_t usermap_pid;
 
@@ -861,3 +917,10 @@ void nsexec(void)
 				case SYNC_USERMAP_PLS:
 					write_log(DEBUG, "stage-1 requested userns mappings");
+					usermap_pid = stage1_pid;
+					if (lrail_single_id()) {
+						if (read(syncfd, &usermap_pid, sizeof(usermap_pid)) != sizeof(usermap_pid)) {
+							sane_kill(stage1_pid, SIGKILL);
+							bail("failed to sync strict single-ID procfs pid from stage-1");
+						}
+					}
 
@@ -874,7 +937,7 @@ void nsexec(void)
 					if (config.is_rootless_euid && !config.is_setgroup)
-						update_setgroups(stage1_pid, SETGROUPS_DENY);
+						update_setgroups(usermap_pid, SETGROUPS_DENY);
 
 					/* Set up mappings. */
-					update_uidmap(config.uidmappath, stage1_pid, config.uidmap, config.uidmap_len);
-					update_gidmap(config.gidmappath, stage1_pid, config.gidmap, config.gidmap_len);
+					update_uidmap(config.uidmappath, usermap_pid, config.uidmap, config.uidmap_len);
+					update_gidmap(config.gidmappath, usermap_pid, config.gidmap, config.gidmap_len);
 
@@ -891,3 +954,11 @@ void nsexec(void)
 						bail("failed to sync with stage-1: read(stage2_pid)");
 					}
+					stage2_procfs_pid = stage2_pid;
+					if (lrail_single_id()) {
+						if (read(syncfd, &stage2_procfs_pid, sizeof(stage2_procfs_pid)) != sizeof(stage2_procfs_pid)) {
+							sane_kill(stage1_pid, SIGKILL);
+							sane_kill(stage2_pid, SIGKILL);
+							bail("failed to sync strict single-ID procfs stage-2 pid from stage-1");
+						}
+					}
 
@@ -912,6 +983,11 @@ void nsexec(void)
 					write_log(DEBUG, "forward stage-1 (%d) and stage-2 (%d) pids to runc",
 						  stage1_pid, stage2_pid);
-					len =
-					    dprintf(pipenum, "{\"stage1_pid\":%d,\"stage2_pid\":%d}\n", stage1_pid,
-						    stage2_pid);
+					if (lrail_single_id()) {
+						len = dprintf(pipenum,
+							      "{\"stage1_pid\":%d,\"stage2_pid\":%d,\"stage2_procfs_pid\":%d}\n",
+							      stage1_pid, stage2_pid, stage2_procfs_pid);
+					} else {
+						len = dprintf(pipenum, "{\"stage1_pid\":%d,\"stage2_pid\":%d}\n", stage1_pid,
+							      stage2_pid);
+					}
 					if (len < 0) {
@@ -986,4 +1062,4 @@ void nsexec(void)
 	case STAGE_CHILD:{
-			pid_t stage2_pid = -1;
+			pid_t stage2_pid = -1, stage2_procfs_pid = -1;
 			enum sync_t s;
 
@@ -1049,5 +1112,10 @@ void nsexec(void)
 				write_log(DEBUG, "request stage-0 to map user namespace");
 				s = SYNC_USERMAP_PLS;
 				if (write(syncfd, &s, sizeof(s)) != sizeof(s))
 					bail("failed to sync with parent: write(SYNC_USERMAP_PLS)");
+				if (lrail_single_id()) {
+					pid_t usermap_pid = lrail_procfs_pid();
+					if (write(syncfd, &usermap_pid, sizeof(usermap_pid)) != sizeof(usermap_pid))
+						bail("failed to sync strict single-ID procfs pid to stage-0");
+				}
 
@@ -1065,9 +1133,13 @@ void nsexec(void)
 						bail("failed to re-set process as non-dumpable");
 				}
 
-				/* Become root in the namespace proper. */
-				if (setresuid(0, 0, 0) < 0)
+				/* Retain setup capabilities with the already-mapped strict identity. */
+				if (lrail_single_id()) {
+					verify_lrail_single_id_identity();
+					restore_lrail_setup_capabilities();
+				} else if (setresuid(0, 0, 0) < 0) {
 					bail("failed to become root in user namespace");
+				}
 			}
 
 			/*
@@ -1107,5 +1188,12 @@ void nsexec(void)
 			write_log(DEBUG, "spawn stage-2");
 			stage2_pid = clone_parent(&env, STAGE_INIT);
 			if (stage2_pid < 0)
 				bail("unable to spawn stage-2");
+			stage2_procfs_pid = stage2_pid;
+			if (lrail_single_id()) {
+				if (read(sync_grandchild_pipe[1], &stage2_procfs_pid, sizeof(stage2_procfs_pid)) != sizeof(stage2_procfs_pid)) {
+					sane_kill(stage2_pid, SIGKILL);
+					bail("failed to read strict single-ID procfs stage-2 pid");
+				}
+			}
 
@@ -1119,5 +1207,11 @@ void nsexec(void)
 			if (write(syncfd, &stage2_pid, sizeof(stage2_pid)) != sizeof(stage2_pid)) {
 				sane_kill(stage2_pid, SIGKILL);
 				bail("failed to sync with parent: write(stage2_pid)");
 			}
+			if (lrail_single_id()) {
+				if (write(syncfd, &stage2_procfs_pid, sizeof(stage2_procfs_pid)) != sizeof(stage2_procfs_pid)) {
+					sane_kill(stage2_pid, SIGKILL);
+					bail("failed to sync strict single-ID procfs stage-2 pid to stage-0");
+				}
+			}
 
@@ -1171,4 +1265,9 @@ void nsexec(void)
 			/* For debugging. */
 			prctl(PR_SET_NAME, (unsigned long)"runc:[2:INIT]", 0, 0, 0);
 			write_log(DEBUG, "~> nsexec stage-2");
+			if (lrail_single_id()) {
+				pid_t procfs_pid = lrail_procfs_pid();
+				if (write(syncfd, &procfs_pid, sizeof(procfs_pid)) != sizeof(procfs_pid))
+					bail("failed to sync strict single-ID procfs pid to stage-1");
+			}
 
@@ -1180,13 +1252,18 @@ void nsexec(void)
 			if (setsid() < 0)
 				bail("setsid failed");
 
-			if (setuid(0) < 0)
-				bail("setuid failed");
+			if (lrail_single_id()) {
+				verify_lrail_single_id_identity();
+				restore_lrail_setup_capabilities();
+			} else {
+				if (setuid(0) < 0)
+					bail("setuid failed");
 
-			if (setgid(0) < 0)
-				bail("setgid failed");
+				if (setgid(0) < 0)
+					bail("setgid failed");
+			}
 
-			if (!config.is_rootless_euid && config.is_setgroup) {
+			if (!config.is_rootless_euid && config.is_setgroup && !lrail_single_id()) {
 				if (setgroups(0, NULL) < 0)
 					bail("setgroups failed");
 			}
diff --git a/libcontainer/container.go b/libcontainer/container.go
index 570060d..d2ab512 100644
--- a/libcontainer/container.go
+++ b/libcontainer/container.go
@@ -48,3 +48,6 @@ type BaseState struct {
 	// InitProcessPid is the init process id in the parent namespace.
 	InitProcessPid int `json:"init_process_pid"`
+
+	// InitProcessProcfsPid is the init process id visible in the inherited procfs mount.
+	InitProcessProcfsPid int `json:"init_process_procfs_pid,omitempty"`
 
diff --git a/libcontainer/init_linux.go b/libcontainer/init_linux.go
index 8e84be7..4356680 100644
--- a/libcontainer/init_linux.go
+++ b/libcontainer/init_linux.go
@@ -38,3 +38,4 @@ type pid struct {
 	Pid           int `json:"stage2_pid"`
 	PidFirstChild int `json:"stage1_pid"`
+	ProcfsPid     int `json:"stage2_procfs_pid,omitempty"`
 }
diff --git a/libcontainer/process_linux.go b/libcontainer/process_linux.go
index fe736ee..a9aef1d 100644
--- a/libcontainer/process_linux.go
+++ b/libcontainer/process_linux.go
@@ -113,4 +113,4 @@ func (p *containerProcess) pid() int {
 func (p *containerProcess) startTime() (uint64, error) {
-	stat, err := system.Stat(p.pid())
+	stat, err := system.Stat(p.container.initProcessStatPid())
 	return stat.StartTime, err
 }
@@ -159,4 +159,5 @@ type setnsProcess struct {
 	rootlessCgroups bool
 	intelRdtPath    string
 	initProcessPid  int
+	initProcfsPid   int
 }
@@ -287,5 +288,5 @@ func (p *setnsProcess) start() (retErr error) {
 			// https://github.com/opencontainers/runc/issues/2356#issuecomment-621277643
 			// Try to join the cgroup of InitProcessPid.
 			if cgroups.IsCgroup2UnifiedMode() && p.initProcessPid != 0 {
-				initProcCgroupFile := fmt.Sprintf("/proc/%d/cgroup", p.initProcessPid)
+				initProcCgroupFile := fmt.Sprintf("/proc/%d/cgroup", p.initProcfsPid)
 				initCg, initCgErr := cgroups.ParseCgroupFile(initProcCgroupFile)
@@ -451,7 +451,7 @@ type initProcess struct {
 // getChildPid receives the final child's pid over the provided pipe.
-func (p *initProcess) getChildPid() (int, error) {
+func (p *initProcess) getChildPid() (int, int, error) {
 	var pid pid
 	if err := json.NewDecoder(p.comm.initSockParent).Decode(&pid); err != nil {
 		_ = p.cmd.Wait()
-		return -1, err
+		return -1, -1, err
 	}
@@ -465,4 +465,8 @@ func (p *initProcess) getChildPid() (int, error) {
 	_, _ = firstChildProcess.Wait()
 
-	return pid.Pid, nil
+	procfsPid := pid.Pid
+	if pid.ProcfsPid > 0 {
+		procfsPid = pid.ProcfsPid
+	}
+	return pid.Pid, procfsPid, nil
 }
@@ -520,6 +524,6 @@ func (p *initProcess) goCreateMountSources(ctx context.Context) (mountSourceReque
 		}
 
 		// Attach to the container's mount namespace.
-		nsFd, err := os.Open(fmt.Sprintf("/proc/%d/ns/mnt", p.pid()))
+		nsFd, err := os.Open(fmt.Sprintf("/proc/%d/ns/mnt", p.container.initProcessStatPid()))
 		if err != nil {
 			errCh <- fmt.Errorf("mount source thread: open container mntns: %w", err)
@@ -665,6 +669,7 @@ func (p *initProcess) start() (retErr error) {
 
-	childPid, err := p.getChildPid()
+	childPid, procfsPid, err := p.getChildPid()
 	if err != nil {
 		return fmt.Errorf("can't get final child's PID from pipe: %w", err)
 	}
+	p.container.initProcessProcfsPid = procfsPid
 
@@ -675,6 +679,6 @@ func (p *initProcess) start() (retErr error) {
 	// can potentially move them (e.g., via dup2()).  If we don't do this now,
 	// we won't know at checkpoint time which file descriptor to look up.
-	fds, err := getPipeFds(childPid)
+	fds, err := getPipeFds(procfsPid)
 	if err != nil {
-		return fmt.Errorf("error getting pipe fds for pid %d: %w", childPid, err)
+		return fmt.Errorf("error getting pipe fds for pid %d: %w", procfsPid, err)
 	}
diff --git a/libcontainer/factory_linux.go b/libcontainer/factory_linux.go
index 267ca4f..70a08c8 100644
--- a/libcontainer/factory_linux.go
+++ b/libcontainer/factory_linux.go
@@ -121,5 +121,9 @@ func Load(root, id string) (*Container, error) {
 	if err != nil {
 		return nil, err
 	}
+	procfsPid := state.InitProcessProcfsPid
+	if procfsPid <= 0 {
+		procfsPid = state.InitProcessPid
+	}
 	r := &nonChildProcess{
 		processPid:       state.InitProcessPid,
@@ -131,4 +135,5 @@ func Load(root, id string) (*Container, error) {
 	c := &Container{
 		initProcess:          r,
+		initProcessProcfsPid: procfsPid,
 		initProcessStartTime: state.InitProcessStartTime,
 		id:                   id,
diff --git a/libcontainer/container_linux.go b/libcontainer/container_linux.go
index 6f42280..574d61b 100644
--- a/libcontainer/container_linux.go
+++ b/libcontainer/container_linux.go
@@ -35,10 +35,11 @@ type Container struct {
 	stateDir             string
 	config               *configs.Config
 	cgroupManager        cgroups.Manager
 	intelRdtManager      *intelrdt.Manager
 	initProcess          parentProcess
+	initProcessProcfsPid int
 	initProcessStartTime uint64
 	m                    sync.Mutex
 	criuVersion          int
 	state                containerState
 	created              time.Time
@@ -228,5 +229,5 @@ func (c *Container) Exec() error {
 
 func (c *Container) exec() error {
 	path := filepath.Join(c.stateDir, execFifoFilename)
-	pid := c.initProcess.pid()
+	pid := c.initProcessStatPid()
 	blockingFifoOpenCh := awaitFifoOpen(path)
@@ -524,6 +524,12 @@ func (c *Container) newParentProcess(p *Process) (parentProcess, error) {
 		cmd.SysProcAttr = &unix.SysProcAttr{}
 	}
 	cmd.Env = append(cmd.Env, "GOMAXPROCS="+os.Getenv("GOMAXPROCS"))
+	if os.Getenv("LRAIL_ROOTLESSKIT_SINGLE_ID") == "true" {
+		if !configs.IsLrailSingleIDMap(c.config.UIDMappings) || !configs.IsLrailSingleIDMap(c.config.GIDMappings) {
+			return nil, errors.New("strict single-ID mode requires the exact logical uid and gid maps")
+		}
+		cmd.Env = append(cmd.Env, "LRAIL_ROOTLESSKIT_SINGLE_ID=true")
+	}
 	cmd.ExtraFiles = append(cmd.ExtraFiles, p.ExtraFiles...)
 	if p.ConsoleSocket != nil {
 		cmd.ExtraFiles = append(cmd.ExtraFiles, p.ConsoleSocket)
@@ -653,6 +659,7 @@ func (c *Container) newSetnsProcess(p *Process, cmd *exec.Cmd, comm *processComm
 		cgroupPaths:     state.CgroupPaths,
 		rootlessCgroups: c.config.RootlessCgroups,
 		intelRdtPath:    state.IntelRdtPath,
 		initProcessPid:  state.InitProcessPid,
+		initProcfsPid:   state.InitProcessProcfsPid,
 	}
 	if len(p.SubCgroupPaths) > 0 {
@@ -899,6 +906,16 @@ func (c *Container) refreshState() error {
 	return c.state.transition(&runningState{c: c})
 }
 
+func (c *Container) initProcessStatPid() int {
+	if c.initProcessProcfsPid > 0 {
+		return c.initProcessProcfsPid
+	}
+	if c.initProcess != nil {
+		return c.initProcess.pid()
+	}
+	return -1
+}
+
 // hasInit tells whether the container init process exists.
 func (c *Container) hasInit() bool {
 	if c.initProcess == nil {
@@ -906,5 +923,5 @@ func (c *Container) hasInit() bool {
 		return false
 	}
-	pid := c.initProcess.pid()
+	pid := c.initProcessStatPid()
 	stat, err := system.Stat(pid)
 	if err != nil {
@@ -927,9 +944,11 @@ func (c *Container) currentState() *State {
 		startTime           uint64
 		externalDescriptors []string
 		pid                 = -1
+		procfsPid           = -1
 	)
 	if c.initProcess != nil {
 		pid = c.initProcess.pid()
+		procfsPid = c.initProcessStatPid()
 		startTime, _ = c.initProcess.startTime()
 		externalDescriptors = c.initProcess.externalDescriptors()
 	}
@@ -945,6 +965,7 @@ func (c *Container) currentState() *State {
 		BaseState: BaseState{
 			ID:                   c.ID(),
 			Config:               *c.config,
 			InitProcessPid:       pid,
+			InitProcessProcfsPid: procfsPid,
 			InitProcessStartTime: startTime,
 			Created:              c.created,
@@ -956,7 +976,7 @@ func (c *Container) currentState() *State {
 		ExternalDescriptors: externalDescriptors,
 	}
-	if pid > 0 {
+	if procfsPid > 0 {
 		for _, ns := range c.config.Namespaces {
-			state.NamespacePaths[ns.Type] = ns.GetPath(pid)
+			state.NamespacePaths[ns.Type] = ns.GetPath(procfsPid)
 		}
 		for _, nsType := range configs.NamespaceTypes() {
@@ -970,6 +991,6 @@ func (c *Container) currentState() *State {
 			}
 			if _, ok := state.NamespacePaths[nsType]; !ok {
 				ns := configs.Namespace{Type: nsType}
-				state.NamespacePaths[ns.Type] = ns.GetPath(pid)
+				state.NamespacePaths[ns.Type] = ns.GetPath(procfsPid)
 			}
 		}
