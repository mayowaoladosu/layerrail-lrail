rules:
  - id: lrail-seeded-canary
    category: LayerRail
    title: LayerRail seeded secret canary
    severity: CRITICAL
    keywords:
      - LRAIL_SECRET_CANARY_
    regex: (?P<secret>LRAIL_SECRET_CANARY_[A-Z0-9]{20,})
    secret-group-name: secret
